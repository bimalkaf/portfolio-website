export interface EducationModel{
    institute: string;
    course : string;
    duration: string;
    score: string;
}

export interface ExperienceModel{
    role: string,
    company: string,
    duration: string,
    description : string[]
}